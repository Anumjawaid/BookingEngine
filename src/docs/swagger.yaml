openapi: 3.0.0
info:
  title: Travel Booking Engine API
  version: 1.0.0
  description: Live documentation for the MERN travel backend.
servers:
  - url: http://localhost:5000/api/v1
    description: Local Dev Server
paths:
  /health:
    get:
      summary: Check system status
      responses:
        '200':
          description: Server is live and running smoothly.